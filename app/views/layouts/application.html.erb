<!DOCTYPE html>
<html>
  <head>
    <title>RailsRuby3</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container">
        <a class="navbar-brand" href="/">Gigih Family Catering</a>
        <ul class="navbar-nav">
          <li class="nav-item">
            <% if controller_path == "menus" %>
            <a class="nav-link active" aria-current="page" href="<%= menus_path %>">Menus</a>
            <% else %>
            <a class="nav-link" href="<%= menus_path %>">Menus</a>
            <% end %>
          </li>
          <li class="nav-item">
            <% if controller_path == "customer_orders" %>
            <a class="nav-link active" aria-current="page" href="<%= customer_orders_path %>">Customer Orders</a>
            <% else %>
            <a class="nav-link" href="<%= customer_orders_path %>">Customer Orders</a>
            <% end %>
          </li>
        </ul>
      </div>
      </div>
    </nav>
    <div class="container mt-3">
      <%= yield %>
    </div>
  </body>
</html>